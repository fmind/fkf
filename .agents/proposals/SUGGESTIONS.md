# FKF and Brain: suggestions

Reviewed **2026-09-05**: FKF `925d5ea`, Brain `9bdaa51`, and existing uncommitted work. **Impact means benefit to daily use, not effort.** Delete unwanted rows before planning.

**Keep:** one binary, provider CLI commands, plain evidence, explicit trust, offline retrieval, bounded MCP, and skills. Harnesses own reasoning and execution. FKF supplies their context; it needs no model runner, daemon, provider SDK framework, or global base registry.

## Seven-day assessment

The August 29–September 4 history contains **19 FKF and 21 Brain commits**. Non-test Go grew **21,734 → 40,637 lines (+87%)**; `da2afeb` touched 315 files. Brain's tracked task files grew **4 → 1,936**, principally through transcript imports. These counts include comments. Trust checks, cache integrity, and hermetic tests earn their complexity; transcript handling, harness setup, provider-specific presentation, and patch machinery deserve simplification.

## Suggested changes

| ID  | What to change                                                                                                                                                                                                                                                                                    | Impact | Benefit / evidence                                                                                                                                                                                                        |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S01 | **Support multiple bases throughout integrations** (FKF + shell). Use distinct MCP registrations, base-neutral skills, workspace-scoped hooks, and base-qualified citations. Show the selected base.                                                                                              | High   | Disposable base B replaced A's registration and skill links. `FKF_BASE` also overrides the current base directory. Prevent context mixing. See `services/harness_plan.go`, `core/discover.go`.                            |
| S02 | **Repair scheduled collection and expose failures** (both). Use a reproducible tool environment; stop assuming `hf` is a Python script. Report the last run separately from timer activation, expose upgrade drift, and retain safe macOS diagnostics.                                            | High   | The active timer's 07:00 run failed: its stale PATH selected a mise shim. Brain status still showed zero errors/warnings. See `services/schedule.go`, `presets/bin/huggingface-repositories-json.sh`.                     |
| S03 | **Keep automatic transcripts as collected evidence** (both). Prefer existing JSON collection for future imports; reserve authored tasks for reviewed summaries. Preserve existing traces, label their origin, and align generated Markdown with Brain's formatter.                                | High   | There are 2,100 task files, 164 untracked imports, and 19 formatting failures blocking normal maintenance. `services/context.go` misleadingly calls imported task content authored. See also `services/session_trace.go`. |
| S04 | **Test retrieval at real delivery budgets** (both). Add top-rank requirements, negative cases, paraphrases, and representative hook/MCP budgets. Require evidence before adding ranking rules.                                                                                                    | High   | All seven Brain evaluations pass, although relevant pages can rank tenth behind unrelated results. `services/eval.go` uses the maximum narrative budget. Prevent overfitting and measure what agents actually receive.    |
| S05 | **Slim the lexical cache and receipts** (FKF). Reduce duplicated candidate/posting data and optional receipt detail, preserving digest checks and indexed/scan equivalence. Keep diagnostics available through `--explain`.                                                                       | Medium | The lexical TSV is **366 MiB**, but the measured query is **3× faster** indexed. Preserve that benefit with less storage and token overhead. See `services/lexical_index*.go`, `services/context_budget.go`.              |
| S06 | **Move provider-specific briefing policy into helpers/skills** (FKF). Retain generic retrieval/day primitives; make authentication probing explicitly live.                                                                                                                                       | Medium | `services/brief.go` and `services/day.go` name particular Google/GitHub sources. Renamed sources and Jira-backed teams should work without new Go adapters.                                                               |
| S07 | **Provide a practical team-base recipe** (FKF). Include scoped Jira/GitHub examples, provider-owned account selection through local launchers, and one collector publishing shared evidence through Git.                                                                                          | High   | `presets/team.yaml` currently offers only a disabled organization repository inventory and session-trace declaration. Support shared context without adding accounts, access-control machinery, or distributed locks.     |
| S08 | **Allow one per-source snapshot-age override** (both). Keep the global fallback; refresh agendas frequently and slow inventories less often.                                                                                                                                                      | Medium | Brain applies one-hour freshness to all index sources. Reduce unnecessary provider calls while retaining the existing scheduler and due-work logic.                                                                       |
| S09 | **Add an opt-in repository-facts helper** (FKF + Brain). Collect roots/remotes, languages, declared build/test commands, and instruction-file pointers without executing repository scripts.                                                                                                      | Medium | Agents still rediscover repository operation despite existing activity collectors. Save deterministic investigation without copying codebases or adding an agent.                                                         |
| S10 | **Define and rehearse encrypted evidence recovery** (Brain). Back up retained historical evidence and curated traces using existing tooling outside FKF; exclude rebuildable caches.                                                                                                              | High   | The Drive mirror excludes events and tasks; ignored evidence never enters Git. Provider history can disappear. Calling all collected evidence reproducible overstates recoverability.                                     |
| S11 | **Simplify learning proposal plumbing** (FKF). Compare a small Git-backed path against the custom parser/archive lifecycle. Adopt only a net reduction preserving exact-byte approval, confinement, validation, and rollback. Keep general skill-evaluation guidance outside FKF's core workflow. | Medium | `services/learn.go` plus `services/learn_diff.go` total 1,311 lines. The pending skill-evolution guide expands the procedural surface further. Avoid maintaining another patch engine.                                    |
| S12 | **Reconcile documentation and shorten onboarding** (both). Align AGENTS/README/skills on aliases, imported evidence, and body retention. Give one setup checklist through scheduling and recovery.                                                                                                | Low    | AGENTS forbids identity-based graph behavior implemented in `services/identity.go` and `services/graph_extract.go`. Consistent instructions prevent agents from undoing valid work.                                       |

## Verification

- **FKF:** static checks, tests, coverage gate, and separate build passed: 1,780 reported tests, one macOS-filesystem skip on Linux, **85.8% coverage**. Secret/vulnerability scans passed. [Exact-HEAD CI](https://github.com/fmind/fkf/actions/runs/33853017860) passed on all four Linux/macOS targets.
- **Brain:** strict validation, **14/14 hooks**, **7/7 evaluations**, and remaining individual checks passed. Aggregate `mise run check` **failed on 19 existing unformatted imports**. No write-formatting `mise run all` was run on either dirty tree.
- **Benchmark:** frozen Brain copies; `context fmind/fkf --budget 1500 --format json`; 3 warmups, 10 runs each. Indexed **0.732 ± 0.043 s**, scan **2.159 ± 0.103 s**, identical semantic results. Linux/i7-1260P, warm caches, shared workstation; an outlier warning limits precision. One workload, not a universal speed claim.
- **Automation:** inspected Linux units/journal, reproduced the HF failure without a provider call, and tested integrations under disposable HOME. Native macOS scheduling remains untested here; logging guidance comes from [Apple](https://developer.apple.com/library/archive/documentation/MacOSX/Conceptual/BPSystemStartup/Chapters/CreatingLaunchdJobs.html).

Review only: no implementation, live collection, harness invocation, scheduler changes, commits, pushes, or uploads. The retained rows can become `.agents/prompts/PLAN.md` after your edits.
